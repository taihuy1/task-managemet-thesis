{
  "name": "task-management-system",
  "version": "1.0.0",
  "description": "Task Management System - Thesis Project",
  "scripts": {
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "api:start": "cd api && npm start",
    "web:start": "cd web && npm start",
    "install:all": "cd api && npm install && cd ../web && npm install",
    "start": "echo 'Starting database...' && docker compose up -d && echo 'Waiting for database...' && sleep 35 && echo 'Starting API server...' && cd api && npm start"
  },
  "keywords": ["task-management", "thesis"],
  "author": "",
  "license": "ISC"
}
